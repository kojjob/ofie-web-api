<div class="bg-gradient-to-b from-blue-50 to-white min-h-screen">
  <!-- Hero Section -->
  <div class="bg-gradient-to-r from-blue-600 to-cyan-600 text-white py-20">
    <div class="container mx-auto px-4">
      <h1 class="text-5xl font-bold mb-6">Explore Neighborhoods</h1>
      <p class="text-xl text-blue-100 max-w-3xl">Discover the perfect neighborhood for your next home with detailed insights on available rentals, pricing, and community features</p>
    </div>
  </div>

  <!-- Search and Filter Section -->
  <div class="container mx-auto px-4 py-8">
    <div class="max-w-4xl mx-auto">
      <div class="bg-white rounded-lg shadow-lg p-6">
        <form class="flex gap-4">
          <div class="flex-1">
            <input type="text"
                   id="neighborhood-search"
                   placeholder="Search by city..."
                   class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                   onkeyup="filterNeighborhoods()">
          </div>
          <button type="button"
                  class="bg-blue-600 text-white px-8 py-3 rounded-lg hover:bg-blue-700 transition-colors font-semibold">
            Search
          </button>
        </form>
      </div>
    </div>
  </div>

  <!-- Neighborhoods Grid -->
  <div class="container mx-auto px-4 py-12">
    <% if @neighborhoods.any? %>
      <div class="max-w-7xl mx-auto">
        <div class="mb-8">
          <h2 class="text-3xl font-bold text-gray-800 mb-2">Featured Neighborhoods</h2>
          <p class="text-gray-600">Browse <%= @neighborhoods.length %> neighborhoods with available rental properties</p>
        </div>

        <div id="neighborhoods-grid" class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% @neighborhoods.each do |neighborhood| %>
            <div class="neighborhood-card bg-white rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition-shadow"
                 data-city="<%= neighborhood.city.downcase %>">
              <!-- Neighborhood Header -->
              <div class="bg-gradient-to-r from-blue-500 to-cyan-500 text-white p-6">
                <div class="flex items-center justify-between mb-2">
                  <h3 class="text-2xl font-bold"><%= neighborhood.city %></h3>
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                </div>
              </div>

              <!-- Neighborhood Stats -->
              <div class="p-6">
                <div class="space-y-4">
                  <!-- Available Properties -->
                  <div class="flex items-center justify-between pb-3 border-b border-gray-200">
                    <div class="flex items-center">
                      <svg class="w-5 h-5 text-blue-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                      </svg>
                      <span class="text-sm font-medium text-gray-700">Available Properties</span>
                    </div>
                    <span class="text-lg font-bold text-blue-600"><%= neighborhood.property_count %></span>
                  </div>

                  <!-- Average Rent -->
                  <div class="flex items-center justify-between pb-3 border-b border-gray-200">
                    <div class="flex items-center">
                      <svg class="w-5 h-5 text-green-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
                      <span class="text-sm font-medium text-gray-700">Average Rent</span>
                    </div>
                    <span class="text-lg font-bold text-green-600">
                      $<%= number_with_delimiter(neighborhood.avg_rent.to_i) %>/mo
                    </span>
                  </div>

                  <!-- Rent Range -->
                  <div class="flex items-center justify-between">
                    <div class="flex items-center">
                      <svg class="w-5 h-5 text-purple-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 12l3-3 3 3 4-4M8 21l4-4 4 4M3 4h18M4 4h16v12a1 1 0 01-1 1H5a1 1 0 01-1-1V4z"></path>
                      </svg>
                      <span class="text-sm font-medium text-gray-700">Rent Range</span>
                    </div>
                    <span class="text-sm font-semibold text-gray-800">
                      $<%= number_with_delimiter(neighborhood.min_rent.to_i) %> - $<%= number_with_delimiter(neighborhood.max_rent.to_i) %>
                    </span>
                  </div>
                </div>

                <!-- View Properties Button -->
                <div class="mt-6">
                  <%= link_to "View Properties",
                      properties_path(city: neighborhood.city),
                      class: "block w-full bg-blue-600 text-white text-center py-3 rounded-lg hover:bg-blue-700 transition-colors font-semibold" %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% else %>
      <!-- Empty State -->
      <div class="max-w-2xl mx-auto text-center py-16">
        <svg class="w-24 h-24 text-gray-400 mx-auto mb-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
        </svg>
        <h2 class="text-3xl font-bold text-gray-800 mb-4">No Neighborhoods Available</h2>
        <p class="text-gray-600 mb-8">We're currently adding new neighborhoods. Check back soon for available rental properties in your area.</p>
        <%= link_to "Browse All Properties", properties_path, class: "inline-block bg-blue-600 text-white px-8 py-3 rounded-lg hover:bg-blue-700 transition-colors font-semibold" %>
      </div>
    <% end %>
  </div>

  <!-- Info Section -->
  <div class="bg-gradient-to-r from-blue-100 to-cyan-100 py-16">
    <div class="container mx-auto px-4 max-w-6xl">
      <h2 class="text-3xl font-bold text-gray-800 mb-12 text-center">Why Choose Your Neighborhood Wisely?</h2>

      <div class="grid md:grid-cols-3 gap-8">
        <div class="bg-white rounded-lg p-8 shadow-lg">
          <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-800 mb-3">Commute & Work</h3>
          <p class="text-gray-600">Consider proximity to your workplace, public transportation options, and average commute times to ensure work-life balance.</p>
        </div>

        <div class="bg-white rounded-lg p-8 shadow-lg">
          <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-800 mb-3">Amenities & Lifestyle</h3>
          <p class="text-gray-600">Look for neighborhoods with shopping, dining, entertainment, parks, and recreational facilities that match your lifestyle.</p>
        </div>

        <div class="bg-white rounded-lg p-8 shadow-lg">
          <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-bold text-gray-800 mb-3">Safety & Community</h3>
          <p class="text-gray-600">Research neighborhood safety ratings, community engagement, and the overall atmosphere to find where you'll feel at home.</p>
        </div>
      </div>
    </div>
  </div>

  <!-- Call to Action -->
  <div class="container mx-auto px-4 py-16">
    <div class="max-w-4xl mx-auto text-center bg-gradient-to-r from-blue-600 to-cyan-600 rounded-2xl p-12 text-white">
      <h2 class="text-3xl font-bold mb-4">Ready to Find Your Perfect Neighborhood?</h2>
      <p class="text-xl text-blue-100 mb-8">Browse thousands of available rental properties in neighborhoods across the country</p>
      <%= link_to "Explore All Properties", properties_path, class: "inline-block bg-white text-blue-600 px-8 py-3 rounded-lg hover:bg-blue-50 transition-colors font-semibold text-lg" %>
    </div>
  </div>
</div>

<script>
  function filterNeighborhoods() {
    const searchTerm = document.getElementById('neighborhood-search').value.toLowerCase();
    const cards = document.querySelectorAll('.neighborhood-card');

    cards.forEach(card => {
      const city = card.dataset.city;

      if (city.includes(searchTerm)) {
        card.style.display = 'block';
      } else {
        card.style.display = 'none';
      }
    });
  }
</script>
